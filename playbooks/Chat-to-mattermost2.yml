---
- name: Enviar POST a webhook con JSON usando Ansible
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Enviar texto a webhook chatops
      ansible.builtin.uri:
        url: "{{ _url }}"
        method: POST
        headers:
          Content-Type: "application/json"
        body_format: json
        body:
          text: 'Se ha ejecutado el Job Template fix mariabd eliminando el parámetro  "{{ _mysql_unknown_option }}" Posteriormente, se reinició el servicio MariaDB y se validó que la base de datos ya se encuentra operando correctamente.'
        validate_certs: false
        return_content: true
      register: response

    - name: Mostrar respuesta del servidor
      ansible.builtin.debug:
        var: response.content
